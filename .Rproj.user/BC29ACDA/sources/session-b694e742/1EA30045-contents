# Load one of R's built-in data sets about car
data("mtcars")

# Fit a straight line for mpg vs hp and plot the result
mpg_model = lm(mpg ~ hp, data = mtcars)
# The x coordinate of the dot represents the horsepower (hp) of that car’s engine.
# The y coordinate represents that car’s gas mileage (mpg).
plot(mtcars$hp, mtcars$mpg)
plot(mtcars$mpg, mtcars$wt)
# Fitting a linear regression model 
# to estimate the systematic relationship between mileage and horsepower.
abline(mpg_model)
# Tell you the y-intercept and slope of the trend line.
coef(mpg_model)